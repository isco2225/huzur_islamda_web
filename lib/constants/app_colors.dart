import 'package:flutter/material.dart';

/// Application color palette.
class AppColors {
  AppColors._();

  /// Primary brand color.
  static const Color primary = Color(0xFF687d2a);

  /// Secondary color.
  static const Color secondary = Color(0xFF000000);

  /// Background color.
  static const Color background = Color(0xFFfaf9eb);

  /// Surface color.
  static const Color surface = Color(0xFFF5F5F5);

  /// Error color.
  static const Color error = Color(0xFFB00020);

  /// Success color.
  static const Color success = Color(0xFF1e4620);

  /// Warning color.
  static const Color warning = Color(0xFFFF9800);

  /// Info color.
  static const Color info = Color(0xFF2196F3);

  /// Border color.
  static const Color border = Color(0x00000019);

  /// Text colors.
  static const Color titleColor = Color(0x00000019);
  static const Color subtitleColor = Colors.black87;
}
